# Base16 Builder Python

If you don't have root access on your computer you should first
make a virtualenv.

To do so:
```
virtualenv base16-venv
source base16-venv/bin/activate
```

Then run

```
pip install -r requirements.txt
python builder.py update
python builder.py
```
